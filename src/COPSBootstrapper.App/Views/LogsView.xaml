<UserControl x:Class="COPSBootstrapper.App.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="28,24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition />
    </Grid.RowDefinitions>

    <TextBlock Text="LOGS" Style="{StaticResource Text.PageTitle}" />

    <Grid Grid.Row="1" Margin="0,10,0,12">
      <StackPanel Orientation="Horizontal">
        <TextBlock Text="Minimum level" Style="{StaticResource Text.Label}" VerticalAlignment="Center" Margin="0,0,8,0" />
        <ComboBox ItemsSource="{Binding LevelOptions}" SelectedItem="{Binding SelectedLevel}" Width="110" />
        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="240" Margin="12,0,0,0"
                 ToolTip="Filter by message or source" />
      </StackPanel>
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource Text.Label}" VerticalAlignment="Center" Margin="0,0,10,0" />
        <Button Style="{StaticResource SecondaryButton}" Content="Open log folder" Command="{Binding OpenLogFolderCommand}" Margin="0,0,8,0" />
        <Button Style="{StaticResource SecondaryButton}" Content="Export" Command="{Binding ExportCommand}" Margin="0,0,8,0" />
        <Button Style="{StaticResource SecondaryButton}" Content="Clear" Command="{Binding ClearCommand}" />
      </StackPanel>
    </Grid>

    <Border Grid.Row="2" Style="{StaticResource Card}" Padding="8">
      <ListBox ItemsSource="{Binding FilteredView}" Background="Transparent" BorderThickness="0"
               ScrollViewer.HorizontalScrollBarVisibility="Disabled"
               VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
        <ListBox.ItemTemplate>
          <DataTemplate>
            <Grid Margin="0,1">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="92" />
                <ColumnDefinition Width="70" />
                <ColumnDefinition Width="170" />
                <ColumnDefinition />
              </Grid.ColumnDefinitions>
              <TextBlock Text="{Binding TimeText}" FontFamily="{StaticResource Font.Mono}" FontSize="11"
                         Foreground="{StaticResource Brush.TextMuted}" />
              <TextBlock Grid.Column="1" Text="{Binding Level}" FontFamily="{StaticResource Font.Mono}" FontSize="11"
                         FontWeight="Bold" Foreground="{Binding Level, Converter={StaticResource LogLevelBrush}}" />
              <TextBlock Grid.Column="2" Text="{Binding Source}" FontFamily="{StaticResource Font.Mono}" FontSize="11"
                         Foreground="{StaticResource Brush.TextSecondary}" />
              <TextBlock Grid.Column="3" Text="{Binding Message}" FontFamily="{StaticResource Font.Mono}" FontSize="11"
                         TextWrapping="Wrap" />
            </Grid>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
    </Border>
  </Grid>
</UserControl>
